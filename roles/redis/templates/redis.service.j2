[Unit]
Description=Redis
Requires=network.target
After=network.target

[Service]
Type=simple
StandardOutput=null
ExecStart=redis-server
User={{ redis_user }}
Group={{ redis_group }}
Restart=on-failure

[Install]
WantedBy=multi-user.target
Alias=redis.service